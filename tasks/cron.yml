- name: install cron job (ansible-pull)
  cron: user="ansible" name="ansible provision" minute="*/10" job="/usr/bin/ansible-pull -o -U https://github.com/deNBI/database_transfer.git -C cvmfs_client_ansible > /dev/null"
